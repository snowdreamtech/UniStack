---
# =====================================================================
# roles/apps/foundation/defaults/main.yml
#
# Purpose:
#   Default variables for the foundation app.
#   (Empty by default, variables are defined in vars/)
# =====================================================================
