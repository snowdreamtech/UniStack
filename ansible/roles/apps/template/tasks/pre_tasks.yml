---
# tasks/pre_tasks.yml for the template package
#
# This is the pre_tasks lifecycle hook for the Ansible role.
# Add your installation, configuration, and setup tasks here.

- name: Example task for template package
  ansible.builtin.debug:
    msg: "Installing {{ app_name }} version {{ app_version }}"
