---
# =====================================================================
# roles/init/vars/darwin.yml
# =====================================================================

# OpenSSH (Skipped on Darwin natively, defined for completeness)
init_openssh_packages: []
init_sshd_service: "com.openssh.sshd"
