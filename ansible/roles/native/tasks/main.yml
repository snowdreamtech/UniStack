---
# =====================================================================
# roles/native/tasks/main.yml
#
# Purpose:
#   L2 Execution Engine for Native Deployments.
#   Acts as the central router for native application lifecycle orchestration.
# =====================================================================

- name: "Native Engine: Initialize execution"
  ansible.builtin.include_tasks: "native_loader.yml"
