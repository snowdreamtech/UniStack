---
# =====================================================================
# roles/core/tasks/main.yml
#
# Purpose:
#   The core role provides foundational tools like os_loader.
#   It does not execute anything by default.
#   Include specific tasks using `tasks_from: os_loader`.
# =====================================================================
